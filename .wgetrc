# The number of seconds to wait until timing out (default is 15 minutes).
timeout = 120

# Number of times to retry when a download fails (default is 20).
tries = 3

# Print HTTP/FTP server response.
server_response = on
